# Archived pages

Preserved snapshots of page components. **Do not modify or import from here.**

Each file is a frozen reference — kept so that future redesigns of the live
page cannot overwrite an earlier design. Every file starts with `// @ts-nocheck`
and a header identifying the commit it was copied from.

| File | Source | Saved from |
|------|--------|-----------|
| `landing-qoves-v1.tsx` | `ui/src/pages/Landing.tsx` | 2026-04-20 · commit 147d1499 |

To restore one of these as the live page, copy its contents back into the
original file (removing the `@ts-nocheck` / archive header) and remove the
archived copy if no longer needed.
